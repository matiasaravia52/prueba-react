languaje: node_js

cache:
  directories:
    - node_modules
    - ~/.npm

node_js:
  - "12"

script:
  - npm install
  #- npm run test
  - npm run build

deploy:
  provider: heroku
  api_key: $HEROKU_KEY
  app: prueba-react
  on:
    all_branches: true